export type Gejala = {
    id: string;
    text: string;
  };
  
  export type Diagnosa = {
    id: string;
    name: string;
  };
  
  
  export type Relasi = {
    id: string;
    name: string;
  };
  